# User Management API

- Use CommonJS modules and the existing Express/MySQL structure.
- Keep SQL parameterized and validate request input at the controller boundary.
- Run `npm run check` after JavaScript changes.
